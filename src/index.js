import PlacesAutocomplete from './PlacesAutocomplete';
import { geocodeByAddress, geocodeByPlaceId, getLatLng, getAddressData } from './utils';

export { geocodeByAddress, geocodeByPlaceId, getLatLng, getAddressData };

export default PlacesAutocomplete;
